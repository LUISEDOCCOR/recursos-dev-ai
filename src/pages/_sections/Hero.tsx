import { Nav } from "../../components/Nav";

export const Hero = () => {
  return (
    <header className="fixed top-0 z-50 h-16 w-full border-b border-neutral-700">
      <Nav />
    </header>
  );
};
